{
  "name": "remont-storage-probe",
  "version": "0.1.0",
  "private": true,
  "description": "Временная функция-зонд: проверка доступности Google/Firebase и Yandex S3 из Yandex Cloud Functions. Удалить после проверки.",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node18 --minify --legal-comments=none --outfile=dist/index.js",
    "local": "node local.js",
    "pack": "npm run build && powershell -NoProfile -Command \"Compress-Archive -Path dist/index.js -DestinationPath dist/probe.zip -Force\""
  },
  "dependencies": {
    "aws4": "^1.13.2",
    "firebase-admin": "^13.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
