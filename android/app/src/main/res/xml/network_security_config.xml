<?xml version="1.0" encoding="utf-8"?>
<!--
  Разрешает cleartext (нешифрованный HTTP) только для локальных адресов
  Firebase Emulator Suite, используемых при разработке (см. AppConfig в
  lib/core/app_config.dart: androidEmulatorHost=10.0.2.2, loopbackHost=127.0.0.1).

  Этот файл подключается через android:networkSecurityConfig ТОЛЬКО в
  src/debug/AndroidManifest.xml и src/profile/AndroidManifest.xml.
  Release-манифест (src/main) его не подключает, поэтому в release-сборке
  cleartext-трафик остаётся запрещён по умолчанию — production Firebase и
  Yandex Object Storage всегда работают по HTTPS и в этом не нуждаются.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
